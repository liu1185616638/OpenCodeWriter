# 发布隐私审计

对最终构建产物解包并检查：

- 开发者用户名、主目录、工作区绝对路径和临时目录。
- `.env`、配置备份、日志、崩溃转储、测试夹具和本地数据库。
- API Key、Token、Cookie、私钥、证书私钥、密码和连接字符串。
- 源码映射、调试符号或内部端点是否超出发布策略。
- 用户数据样本、真实邮箱、手机号、地址和唯一标识。
- 安装脚本是否请求超出功能所需的权限。

任何命中都先判定来源和必要性。未证明安全前阻止发布；修复后废弃旧产物，重新构建并重新审计。
